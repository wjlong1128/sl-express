spring:
  data:
    mongodb:
      host: ${mongodb.host}
      port: ${mongodb.port}
      database: ${mongodb.database}
      authentication-database: ${mongodb.authentication-database} #认证数据库
      username: ${mongodb.username}
      password: ${mongodb.password}
      auto-index-creation: ${mongodb.auto-index-creation} #自动创建索引